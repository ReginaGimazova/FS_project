import React from 'react'
import MainTemplate from '../../templates/MainTemplate'
import CommonContent from '../../organisms/CommonContent'

const HomePage = () => (
    <MainTemplate>
        <CommonContent>home</CommonContent>
    </MainTemplate>
);

export default HomePage
